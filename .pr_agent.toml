# NOTE: This file is consumed by Qodo's "free for open source" agent
# (the `qodo-free-for-open-source-projects` bot) — a separate, hosted, closed-source
# service. It is NOT read by the open-source pr-agent in this repository; some keys
# below (e.g. the [review_agent] section) only exist in that hosted agent.

[pr_code_suggestions]
num_code_suggestions_per_chunk = 8   # suggestions asked from AI per diff chunk (default: 3)
max_number_of_calls = 8              # max diff chunks processed (default: 3)
final_clip_factor = 1.0              # don't discard any suggestions (default: 0.8)
suggestions_score_threshold = 0      # keep all scored suggestions (default: 0)

[pr_reviewer]
enable_review_labels_effort = true
enable_auto_approval = true

[github_app]
pr_commands = [
    "/describe --pr_description.publish_description_as_comment=true",
    "/improve",
    "/agentic_review"
]

handle_push_trigger = true
push_commands = [
    "/improve",
    "/agentic_review"
]

[review_agent]
enabled = true
publish_output = true
demand_self_review = true
approve_pr_on_self_review = true
